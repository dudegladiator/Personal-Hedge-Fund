# .streamlit/secrets.toml

# WARNING: Example credentials for testing ONLY.
# Passwords should be securely hashed and stored in a proper database
# in a real application, not in plain text here.

[users]
"alice" = "wonderland"
"bob" = "builder"
"charlie" = "chocolate"
"demo_user" = "password123"
"test_admin" = "adminpass"

# You can add more users following the same pattern:
# "new_username" = "new_password"
