[project]
name = "hedge-fund-manager"
version = "1.0.0"
description = "AI Based Hedge Fund Manager"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain==0.3.20",
    "langgraph==0.3.5",
]
